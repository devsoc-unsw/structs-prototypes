import { DrawableElement } from "../drawable/drawable";
import { LinkedListDataState } from "../type/state";

export function parseLinkedListState(state: LinkedListDataState): Set<DrawableElement> | null {
  console.log(state);
  return null;
}