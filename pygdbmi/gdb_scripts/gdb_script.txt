
define plist
  set var $n = list->head
  set logging file ll_output_json.txt
  set logging on
  printf "["
  while $n
    printf "{\"Data\": %d, \"Address\": \"%p\", \"Next\": \"%p\"}", $n->data, $n, $n->next
    set var $n = $n->next
    if $n
      printf ","
    end
  end
  printf "]\n"
  set logging off
end

file linked_list
break 53
run
plist
continue
quit
